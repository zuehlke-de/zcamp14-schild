console.log('Alive.');

var net = require('net');

var localSocket = net.Socket();
localSocket.connect("/tmp/ble_coupling");
console.log('connected');

var name = "Kurzer Raum :-)"
console.log('length=' + name.length);

var buffer = Uint8Array(2);
buffer[0] = 0;
buffer[1] = name.length;
localSocket.write(Buffer(buffer));
localSocket.write(Buffer(name));
localSocket.end();



