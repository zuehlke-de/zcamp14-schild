console.log('Alive.');

var net = require('net');

var localSocket = net.Socket();
localSocket.connect("/tmp/ble_coupling");
console.log('connected');

var buffer = Uint8Array(4);
//buffer[0] = 1;
//buffer[1] = 2;
//buffer[2] = 'k';
//buffer[3] = 'A';
for (i = 0; i < 4; ++i)
    localSocket.write(buffer[i]);
localSocket.end();



